# Loads every dashboard .json in this directory (kaeos.json).
apiVersion: 1
providers:
  - name: kaeos
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
      foldersFromFilesStructure: false
