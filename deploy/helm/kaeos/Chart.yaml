apiVersion: v2
name: kaeos
description: KAEOS - the governed autonomous-enterprise operating system (backend API + frontend SPA).
type: application
# Chart version tracks the chart; appVersion tracks the app image tag default.
version: 0.1.0
appVersion: "1.4.1"
kubeVersion: ">=1.25.0-0"
maintainers:
  - name: KAEOS
sources:
  - https://github.com/Daksh-Aneja-Projects/KAEOS
