"""HR V1 sub-routers, one module per sub-domain.

app/hr/api/v1/router.py mounts these in declaration order; the module
list there is the route order, so a route's match precedence is still
readable in one place.
"""
